/** Viewport width at which the layout switches from mobile to desktop. */
export const DESKTOP_BREAKPOINT = 768;

/** Default map center [lng, lat] — Singapore. */
export const DEFAULT_MAP_CENTER: [number, number] = [103.795, 1.376];

/** Default map zoom level. */
export const DEFAULT_MAP_ZOOM = 9.5;
